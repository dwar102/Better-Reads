* {
    font-family: 'Open Sans', sans-serif;
    font-size: 16px;
    color: #4c4c4c;
}

.container {
    padding: 30px;
    align-items: center;
    box-shadow: 0 4px 30px rgba(0,0,0,.05);
      border-radius: 8px;
}
.innerContainer {
    align-items: center;
}

td {
    text-align: right;
}

button {
    background-color: #3abfbb;
}